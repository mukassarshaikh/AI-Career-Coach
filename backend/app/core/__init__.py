# core package — import from submodules directly, e.g.:
#   from app.core.config import settings
#   from app.core.db import AsyncSessionLocal, Base
#   from app.core.security import get_email_from_token
