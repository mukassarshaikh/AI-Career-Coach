"""
embedding_service.py — Shared embedding generation wrapper (Phase 1).
Generates vectors for skill text → pgvector storage.
Dimension must match skill_vectors.vector and market_skill_reference.vector (384).
"""
# TODO: implement in Phase 1
